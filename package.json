{
  "name": "bhh-drug-allergy-assessment",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test",
    "check": "find src -name \"*.js\" -print0 | xargs -0 -n1 node --check",
    "build": "node scripts/build-css.mjs && npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
